Q1: react => virtual dom => reconcilation + diffing algo

div
    h1 P
        SPAN1

version 1

div
    h1 P
        SPAN

version 2
div
    h1 P
        SPAN1


Q2:difference ?

<div class="row">
<> </>
<div class="col">
</div>

<>

</>

// learn:

const fn = () => {}
function vs arrow function
anonymous function
synthetic events
hook
onclick

parameter vs argument
callback function 


Redux:

context api: 

prop drilling / prop lifting:

props => send data from parent to child .
callback function + props => send data from child to parent

                App (parent)
        Home            About 
Heading    Button   Input   Button
                    Heading 

plain fetch + reducer function => use for small apps.

// createAsyncThunk => (If you are doing async business logic)
This automatically manages:

1. loading state

2. success state

3. error state

Ex:
multi-step workflow (login → save token → redirect → show toast)
not tied to a cached resource (upload image → track % progress)
needs custom control flow (call 3 APIs in sequence)
triggers multiple slice
background job 
payment flow & checkout process

// Best option for API fetching: RTK Query (If you are fetching server data to display & CACHING)
Auto refetch on focus or reconnects/ cache
Offline handling
track loading/error
auto dedupe request


createAsyncThunk (generic async workflow) , rtk query (cache) (api call)  => async task 

fetching data
crud api
caching => 4 fetchUsers() => 
automatically refetch => on focus , on network re-connect, polling 
want tags
deduplication of requests


multiple api calls in seq; 
/login => token => dashbaord => user/profile => (business workflows)
uploading files with custom progress 
background job (localstorage + call api)
slice => reducer(api) , slice 1=> reducer (api)
abortController => api request cancel 